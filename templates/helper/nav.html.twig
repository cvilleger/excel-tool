<nav class="white" role="navigation">
    <div class="nav-wrapper container">
        <a id="logo-container" href="{{ path('app_home') }}" class="brand-logo">Excel Tool</a>
        <ul class="right hide-on-med-and-down">
            {% if app.user %}
                <li><a href="{{ path('app_dashboard') }}">Dashboard</a></li>
                <li><a class="modal-trigger" href="#modal-notification"><i class="material-icons">notifications</i></a></li>
                <li><a href="{{ path('app_notification') }}">Notification (To Remove)</a></li>
                <li><a href="{{ path('app_logout') }}">Logout</a></li>
            {% else %}
                <li><a href="{{ path('app_register') }}">Register</a></li>
                <li><a href="{{ path('app_login') }}">Login</a></li>
            {% endif %}
        </ul>

        <ul id="nav-mobile" class="sidenav">
            {% if app.user %}
                <li><a href="{{ path('app_dashboard') }}">Dashboard</a></li>
                <li><a class="modal-trigger" href="#modal-notification"><i class="material-icons">notifications</i></a></li>
                <li><a href="{{ path('app_notification') }}">Notification</a></li>
                <li><a href="{{ path('app_logout') }}">Logout</a></li>
            {% else %}
                <li><a href="{{ path('app_register') }}">Register</a></li>
                <li><a href="{{ path('app_login') }}">Login</a></li>
            {% endif %}
        </ul>
        <a href="#" data-target="nav-mobile" class="sidenav-trigger"><i class="material-icons">menu</i></a>
    </div>
</nav>

{% if app.user %}

<div id="modal-notification" class="modal">
    <div class="modal-content">
        <h4>Notification Center</h4>
        <ul class="collection">
            {% for file in app.user.files %}
            <li class="collection-item avatar">
                <i class="material-icons circle">folder</i>
                <span class="title">{{ file.name }}</span>
                <p>In Progress<br>
                    42% of {{ file.size }}
                </p>
                <a href="#!" class="secondary-content"><i class="material-icons">grade</i></a>
            </li>
            {% else %}
                <li class="collection-item">
                    No Notification
                </li>
            {% endfor %}
            <li class="collection-item avatar">
                <i class="material-icons circle green">insert_chart</i>
                <span class="title">Title</span>
                <p>First Line <br>
                    Second Line
                </p>
                <a href="#!" class="secondary-content"><i class="material-icons">grade</i></a>
            </li>
            <li class="collection-item avatar">
                <i class="material-icons circle red">play_arrow</i>
                <span class="title">Title</span>
                <p>First Line <br>
                    Second Line
                </p>
                <a href="#!" class="secondary-content"><i class="material-icons">grade</i></a>
            </li>
        </ul>
    </div>
    <div class="modal-footer">
        <a href="" class="modal-close waves-effect waves-green btn-flat">Ok</a>
    </div>
</div>

{% endif %}

{% for label, messages in app.flashes %}
    {% for message in messages %}

    {% endfor %}
{% endfor %}
